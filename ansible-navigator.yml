--- 
ansible-navigator: 
  mode: stdout

  execution-environment: 
      enabled: true 
      image: docker.io/jellebens/ansible-excecution-environment:1.0.3   
      container-engine: podman 
      volume-mounts:
        - src: ${PWD}
          dest: "/navigator/"
          options: "Z"
      environment-variables:
        set: 
          ANSIBLE_CONFIG: /navigator/ansible.cfg
  ansible: 
    config: 
      help: false 
      path: /navigator/ansible.cfg 

  playbook-artifact: 
    enable: true 
    save-as: /navigator/artifacts/playbooks/{playbook_name}/artifact-{time_stamp}.json 
    
  
    