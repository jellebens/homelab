#SPDX-License-Identifier: MIT-0
---
- name: Ensure SSH keys are available on control node
  import_tasks: prepare-control-node.yml

- name: Configure ssh on host
  import_tasks: configure-ssh-host.yml

